# 個人の Apps Script プロジェクト ID
# .clasp.json はプレースホルダの状態でリポジトリに含めているため除外しない。
# 自分のスクリプトIDを書き込んだあとは，値がコミットされないよう注意すること。
#   git update-index --skip-worktree .clasp.json
# .clasp.json

# 個人の設定値（Webhook URL / スプレッドシートのURL / Discord ID）
# gas/config_roster.js も同じくプレースホルダの状態で含めている。
#   git update-index --skip-worktree gas/config_roster.js

# clasp の認証情報
.clasprc.json

# 画像など
*.HEIC
*.heic
